---
title: Healthcare Clinic ERP and Analytics Platform
outcomeTitle: "A clinic's data, modelled so the reports stop disagreeing"
seoTitle: "Healthcare clinic ERP and analytics"
summary: >-
  A 10-table 3NF clinical schema with provider, billing, and diagnosis reporting,
  plus a live SQL viewer tracing every dashboard metric back to its source table.
featured: true
order: 2
role: Schema design, reporting layer, and REST API
timeframe: null
stack:
  - SQL
  - Node.js
  - Express
  - React
domains:
  - Data modelling
  - Analytics and reporting
metrics:
  - value: '10'
    label: tables, 3NF
  - value: '5,000'
    label: patients loaded
  - value: '12,000'
    label: appointments loaded
repo: https://github.com/darpanradadiya/healthcare-clinic-erp
repoNote: null
demo: https://healthcare-clinic-erp.onrender.com
screenshot: null
dataNote: >-
  The 5,000 patients and 12,000 appointments are generated data, loaded to validate
  query performance and schema constraints at scale. No real clinical records were
  used.
---

{/*
  TODO(darpan) — this case study exists to prove you can model data, which is the
  single most asked-about skill in analytics engineering interviews.

  Facts already verified from your resume, safe to use:
    - 10-table clinical schema in 3NF
    - Covers patients, providers, encounters, ICD-10 diagnoses, invoices, payments
    - Documented ERD
    - Loaded with 5,000 patients and 12,000 appointments to validate query
      performance at scale
    - Provider-productivity, billing, and clinical-diagnosis reporting layers
    - KPI dashboards with a live SQL viewer tracing metrics back to source tables
    - Full CRUD REST API served to a React front end, with CSV export

  Data provenance is confirmed and disclosed in `dataNote`: the 5,000 patients and
  12,000 appointments are generated, loaded to validate query performance at scale.
  That renders at the top of the case study. Restate it in Context in your own
  words — volunteering it is what makes the schema work credible.
*/}

## Context

A clinic administration system built for ITC 6000, Database Management Systems, at
Northeastern. Ten tables in third normal form, a REST API over them, and a React front
end that runs the whole clinic workflow: registry, scheduling, encounters, ICD-10
diagnoses, invoices and payments.

The data is generated. `server/seed.js` builds 5,000 patients with portal accounts for
about 60% of them, then 12,000 appointments and the encounter, invoice and payment
records that hang off those — using Faker, not a real patient record anywhere. It's
there to make query performance and the constraints mean something at a size a
hand-typed fixture can't reach.

## The problem

[INFERRED] A schema is easy to draw and hard to trust. The interesting question isn't
whether the tables exist, it's whether a number on a dashboard is the same number the
database holds — and with billing split across invoices and payments, and diagnoses
joined through a bridge table, there are several places for those to drift apart.
[INFERRED — confirm or replace: the assignment brief would say what you were actually
asked to solve.]

What the code is built to answer is that question directly: every page can show the
exact SQL that produced what you're looking at.

## Constraints

**Foreign keys are enforced, not decorative.** `PRAGMA foreign_keys = ON` at the top of
the schema, and every relationship is declared. SQLite doesn't do this by default, so
without that line the constraints are documentation.

**The interesting cardinalities are one-to-one, and they're enforced by UNIQUE, not by
convention.** A portal account belongs to exactly one patient; an encounter belongs to
exactly one appointment; an invoice belongs to exactly one encounter. Each is a
`NOT NULL UNIQUE` foreign key, so the database refuses a second one rather than
trusting the API not to write it.

**Status fields are constrained to their real domains.** Appointments are checked
against `Scheduled / Checked In / Completed / Cancelled / No-Show`, encounters against
`Open / Signed`, invoices against `Open / Partial / Paid`, payments against
`Card / Cash / Check / Insurance`. Money is checked too: totals and amounts paid must
be non-negative, and a payment must be strictly positive.

**A diagnosis can only be attached to an encounter once.** `UNIQUE (FK_ENCOUNTER_ID,
FK_DIAGNOSIS_ID)` on the bridge table, with `IS_PRIMARY` as a flag rather than a
separate table.

**The browser gets its own database.** The SQL viewer runs SQLite in WebAssembly via
sql.js against an 8-patient sample committed to `public/`, so the viewer works on a
static deploy with no server. The full 5,000-patient database is built by the seed
script server-side. Why there are two is under Decisions.

## What I built

<ErdDiagram />

Ten tables, drawn above from the schema rather than from a screenshot of it.

The API is Express over better-sqlite3 — 41 routes. Full CRUD on patients, providers,
locations, appointments, encounters and invoices; nested reads for a patient's
appointments, encounters and invoices; payment recording against an invoice;
diagnosis attach and detach on an encounter; and a global search across patients and
providers.

Four of those routes exist only to serve the dashboard, and they're the ones doing
real SQL rather than row lookups: KPI totals, revenue by provider, appointment status
distribution, and a twelve-month revenue trend. Two more (`/api/meta/schema` and
`/api/meta/queries`) exist to feed the schema view and the SQL viewer — the
introspection that makes the traceability claim checkable rather than asserted.

The front end is React on Vite, with Recharts for the charts, shadcn and Radix for the
components, and a `Cmd+K` command palette over the search endpoint. Provider
productivity is computed as signed visits, gross billed, collected, and collection
rate. Billing shows an invoice ledger filterable by status with a payment workflow
behind it. CSV export takes a column selection, a scope of current page or all
matching rows, and the active filters.

It's deployed on Render via `render.yaml`.

## Decisions

### 3NF over a star schema

Reporting was half the point of this system, which is the usual argument for a star
schema — denormalise, accept the redundancy, make the reads fast.

I went with 3NF anyway, because the other half of the point was that the reports had
to agree with each other. In a star schema the same fact can be stored in more than one
place, and once that's true, two reports can both be correct and still disagree. That's
exactly the failure this system existed to eliminate.

3NF means every fact lives in one table and every relationship is a foreign key. Nine
foreign keys, `PRAGMA foreign_keys = ON`, all three one-to-one relations enforced by
`UNIQUE` rather than convention, status fields constrained by `CHECK`, and money
constrained to be non-negative on invoices and strictly positive on payments. A report
that disagrees with another report is a query bug, not a data bug, and query bugs are
findable.

That's also why there's a live SQL viewer tracing each dashboard metric back to its
source table. If someone questions a number, the answer is the query, not an
explanation.

The tradeoff is joins. Provider productivity crosses appointment, encounter, and
provider; billing crosses encounter, invoice, and payment. At 5,000 patients and
12,000 appointments that's not a problem. At a real clinic's volume I'd expect to add a
reporting layer on top rather than denormalise underneath.

{/*
  [CHECK] — the live SQL viewer is on the resume. I've framed it as flowing from the
  3NF decision, which is a reasonable reading but a reading. Confirm.
*/}

### The sql.js split

The demo runs SQL in the browser through sql.js, and the database file it loads has
eight patients in it. The real one has five thousand.

sql.js loads the entire database into browser memory as a single file, so committing
the full seed would mean a multi-megabyte binary in the repository that every visitor
downloads before the page is usable — to run queries whose answers a dashboard already
displays. The eight-patient file proves the viewer works and the queries are real. The
5,000-row database is built server-side by the seed script, which is where the
query-performance claims come from.

So there are two databases doing two jobs: one to demonstrate that metrics trace back
to source tables, one to validate that the schema holds up at scale.

{/*
  [CHECK] — I've written this as deliberate. If the 8-patient file is just what
  happened to get committed, say so instead; "the committed sample is small because it
  was convenient, and the real seed runs server-side" is a fine sentence and a true
  one.
*/}

## Results

The structure is measured. The performance isn't, and I'd rather say that than
estimate it.

Measured: ten tables, nine foreign keys, 41 API routes, 5,000 patients and 12,000
appointments generated by the seed script. The live deployment currently reports
1,231 scheduled appointments, $3,805,719.49 billed and $693,899.01 outstanding — real
figures from the running database, not from a slide.

Not measured: query timings. Nothing in the repository records them — no benchmark, no
`EXPLAIN QUERY PLAN` output, no timing instrumentation on the dashboard endpoints. The
row counts exist so the joins run against a realistic volume, and the joins do run,
but "how fast" is a question I never asked in a way that produced a number. Publishing
one now from a single laptop run would be a worse answer than admitting the gap.

## Limitations

**There is no authentication.** Every one of the 41 routes is open. The schema has a
`PORTAL_ACCOUNT` table with a `PASSWORD_HASH` column, but nothing in the server reads
it — no login endpoint, no session, no token, no password hashing library in the
dependencies. The hashes are Faker output. So the portal accounts model the *shape* of
patient login and implement none of it, and there are no roles either: a receptionist,
a physician and a billing clerk would all see the same everything.

**There is no audit trail.** No table records who changed what. Nothing in the schema
carries a created-at or updated-at column, so an appointment's status can move from
Scheduled to Completed and the row afterwards looks exactly as if it had always been
Completed. For scheduling that's inconvenient; for a clinical note or an invoice it's
disqualifying.

**Diagnoses are coded, procedures aren't.** There's an ICD-10 reference table and a
bridge joining it to encounters, which is the diagnosis half of clinical coding. The
billing half is missing: no CPT or HCPCS table, and no invoice line items at all. An
invoice carries a single `TOTAL_AMOUNT` with nothing itemising how it was arrived at,
which is the one thing a real clinic's billing has to be able to defend.

**It's an embedded database, and writes are single-process.** SQLite through
better-sqlite3, which is synchronous. Writes are wrapped in transactions where they
span tables, so a half-written invoice-and-payment isn't a risk — but WAL mode isn't
enabled and no busy timeout is set, so the concurrency story is "one Node process,
one writer at a time" rather than anything a clinic could run several front desks
against.

**There are no tests.** The `test` script in `package.json` still exits with an error.
That's worth saying plainly on a site whose other case study is about a test suite:
the schema's constraints do a lot of the work a test would, and constraints only catch
what you thought to declare.

**What a real clinic would need that this doesn't:** authentication with roles, an
audit trail on every clinical and financial row, procedure coding and itemised
invoices, insurance and eligibility, a server database with a concurrency model, and
somewhere to be when the database is down. This is a schema-design project that
behaves like an application, not an application.

## Stack and links

Repository and live demo are both linked above. The demo runs on Render's free tier,
so the first request after it has been idle takes about 20 seconds to wake — measured
at 22.3 s from cold, then sub-second.
